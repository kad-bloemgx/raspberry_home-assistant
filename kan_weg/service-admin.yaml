kind: Service
apiVersion: v1

metadata:
  name: openvpn-admin
  namespace: {{ .Values.nameSpace }}
  labels:
    app: {{ .Values.appName }}
spec:
  selector:
    app: {{ .Values.appName }}
  type: NodePort
  ports:
    - name: openvpn-adm
      port: {{.Values.container.portAdmin}}
      protocol: TCP
      targetPort: {{.Values.container.portAdmin}}
      nodePort: 30050