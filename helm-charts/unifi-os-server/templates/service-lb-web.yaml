apiVersion: v1

kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.nameSpace }}

spec:
  ports:
    - protocol: TCP
      name: webui-svc
      port: 443
  type: LoadBalancer
  selector:
    app: unifi-os-server-webui-svc