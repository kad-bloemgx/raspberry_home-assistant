apiVersion: v1
kind: Service
metadata:
  name: unifi-service
  namespace: {{ .Values.nameSpace }}
  labels:
    app: unifi-service
spec:
  type: NodePort
  ports:
    - port: 443
      targetPort: 443
      nodePort: 31443
      protocol: TCP
  selector:
    app: {{ .Values.appName }}