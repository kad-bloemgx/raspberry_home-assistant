apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "longhorn.fullname" . }}-test-connection"
  labels:
    {{- include "longhorn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "longhorn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
