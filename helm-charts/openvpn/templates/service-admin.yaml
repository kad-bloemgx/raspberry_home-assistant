kind: Service
apiVersion: v1

metadata:
  name: openvpn-admin
  namespace: {{ .Values.nameSpace }}
  labels:
    app: {{ .Values.appName }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - protocol: TCP
      port: {{ .Values.container.portAdmin }}
      name: web


